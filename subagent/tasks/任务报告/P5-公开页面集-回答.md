# 任务报告 - P5 公开页面集

## A. 任务前自检（Step 2 答题）
1. 对应原型：
   - `小程序原型图/index.html` + `小程序原型图/index.png`
   - `小程序原型图/help.html` + `小程序原型图/help.png`
   - `小程序原型图/event-apply.html` + `小程序原型图/event-apply.png`
   - 说明：本轮已按你的纠偏，把入驻申请统一改回 `event-apply.html / event-apply.png` 为准；`apply-status` 无独立原型，我基于公开查询要求延展实现。
2. 调用接口：
   - `POST /jst/public/organizer/partner/apply`
   - `GET /jst/public/organizer/partner/apply/{applyNo}/status`
   - 文档来源：任务卡指定为 `27-用户端API契约.md §3.10`，但当前 27 文档未落到这两个公开接口；本次已额外对齐后端 `PublicPartnerApplyController`、`PartnerApplyReqDTO`、`PartnerApplyStatusResVO`。
3. 复用 store / api 模块：
   - `jst-uniapp/api/request.js`
   - `jst-uniapp/api/notice.js`
   - 首页继续复用现有 banner / 公告 / 热门赛事加载逻辑
   - 本任务未新增 Pinia store，也未依赖登录态
4. 新建页面 / 组件 / 模块：
   - `jst-uniapp/api/public.js`
   - `jst-uniapp/pages-sub/public/partner-apply.vue`
   - `jst-uniapp/pages-sub/public/apply-status.vue`
   - `jst-uniapp/pages-sub/public/help.vue`
5. 数据流：
   - 首页 onShow → 继续拉 banner / 公告 / 热门赛事 → 新增“服务入口”只做页面跳转
   - 入驻页填写表单 → `submitPartnerApply()` → 返回 `applyNo` → 跳转状态查询页
   - 状态页输入 `applyNo` → `queryApplyStatus()` → 展示 `applyStatus / auditRemark / submitTime / auditTime`
   - 帮助中心为静态 FAQ / 联系方式展示，无业务接口回传
6. 双视角问题：
   - 否。本任务 3 个新页面均为公开页，免登录，不区分学生 / 家长 / 渠道 / 老师视角。
7. 复用模板：
   - `jst-uniapp/pages/login/login.vue`：顶部 hero + 卡片化表单布局写法
   - `jst-uniapp/pages/notice/list.vue`：搜索 / 分组 / 空状态的组织方式
   - `jst-uniapp/pages-sub/contest/detail.vue`：自定义导航栏与信息卡样式
8. PNG 关键视觉值（按原型观察）：
   - 首页主色：橙色 `#FF6B35`，品牌蓝约 `#1058A0`
   - 帮助 / 入驻页深蓝主色约 `#25328F ~ #0F4D95`
   - 卡片圆角：约 `12px~14px`，换算为 `24rpx~28rpx`
   - 关键留白：页边距约 `12px`，换算为 `24rpx`
   - 搜索框 / 按钮高度：约 `38px~44px`，换算为 `76rpx~88rpx`

## B. 交付物清单
新增文件：
- `jst-uniapp/api/public.js`
- `jst-uniapp/pages-sub/public/partner-apply.vue`
- `jst-uniapp/pages-sub/public/apply-status.vue`
- `jst-uniapp/pages-sub/public/help.vue`
- `subagent/tasks/任务报告/P5-公开页面集-回答.md`

修改文件：
- `jst-uniapp/pages/index/index.vue`
- `jst-uniapp/pages.json`

## C. 静态验收结果
1. 已在首页新增“服务入口”模块，包含“赛事方入驻”“联系客服”两个 2 列卡片入口。
2. `partner-apply.vue` 已按真实后端 DTO 对齐：
   - `partnerName`
   - `contactName`
   - `contactMobile`
   - `businessLicenseNo`
   - `qualificationJson`
   - `settlementInfoJson`
   - `invoiceInfoJson`
   - `contractFilesJson`
3. 资质图片上传按任务卡要求采用前端占位方案：
   - 页面支持本地选图与预览
   - 提交时转换为 mock URL 数组写入 `qualificationJson`
4. `apply-status.vue` 支持：
   - 手输申请单号查询
   - 带 `applyNo` 参数自动查询
   - 最近查询历史复用
5. `help.vue` 已完成：
   - 搜索
   - FAQ 分类
   - 联系方式卡片
   - 协议 / 隐私入口占位
6. `pages.json` 已恢复为合法 JSON，并成功通过 `ConvertFrom-Json` 静态解析。
7. 检查结果确认：
   - 新增公开页未直接写 `uni.request`
   - 公开接口统一走 `api/public.js`

## D. 视觉对比
1. 本轮实际查看的 PNG：
   - `D:/coding/jst_v1/小程序原型图/index.png`
   - `D:/coding/jst_v1/小程序原型图/help.png`
   - `D:/coding/jst_v1/小程序原型图/event-apply.png`
2. 从 PNG 提取的关键视觉值：
   - 首页主色：橙色约 `#FF6B35`，帮助页客服卡蓝约 `#0F4D95`，入驻页主色绿约 `#1B5E20 ~ #2E7D32`
   - 卡片圆角：主卡片约 `12px~14px`，换算约 `24rpx~28rpx`
   - 关键间距：页边距约 `12px`，卡片内边距约 `12px~14px`，换算约 `24rpx~28rpx`
   - 搜索框 / 底部按钮高度：约 `38px~44px`，换算约 `76rpx~88rpx`
3. 因 PNG 与 HTML 偏差做的调整：
   - 首页没有保留中部“大服务区”模块，而是改成更接近 `index.png` 的底部绿色入驻条，并补上右下角客服浮钮。
   - 帮助中心未照搬 HTML 的较重阴影，而是按 `help.png` 改成更轻的卡片阴影、更浅的搜索底色和更紧凑的 2 列联系方式卡片。
   - 入驻申请页曾一度偏向审核页配色，但现已回到 `event-apply.png` 的绿色顶栏、白底步骤条、白卡片和灰+绿双按钮底栏。
   - `apply-status` 虽无独立 PNG，但已按帮助页与入驻页的卡片尺度统一到同一套公开页视觉体系。

## E. 遗留 TODO
- 本轮未接入真实文件上传接口，资质图片仅生成 mock URL；这是任务卡明确允许的阶段性处理。
- 协议 / 隐私目前为入口占位，后续可接正式 H5 文档页。
- 未在本机 HBuilderX / 微信开发者工具完成真机或浏览器联调，本报告的交互结论属于静态验收结论。

## F. 我做了任务卡之外的什么
- 发现并修复了 `jst-uniapp/pages.json` 的历史编码损坏问题，避免新增路由后整个路由文件失效。
- 状态查询页补充了“最近查询历史”，方便重复核对多个申请单号。

## G. 自检确认
- [x] 没有页面内 mock 业务数据
- [x] 所有业务接口通过 `api/request.js`
- [x] 未新增依赖
- [x] 未修改 `RuoYi-Vue/`
- [x] 未修改 `架构设计/`
- [x] 公开页免登录
- [x] 路由已登记到 `pages.json`
- [x] `pages.json` 已静态解析通过
