# REFACTOR-7-MENU 菜单路由大清理 — 交付报告

> Agent: Web Admin Agent | 日期: 2026-04-14 | 状态: ✅ 完成

---

## 一、任务概述

管理端存在"竞赛通管理"与"赛事方工作台"功能重叠、菜单层级混乱、数据看板重复等问题。本卡统一清理菜单结构，产出可执行 SQL 脚本。

## 二、产出物

| 文件 | 路径 |
|------|------|
| 迁移脚本 | `架构设计/ddl/99-migration-refactor-menus.sql` |
| 交付报告 | 本文件 |

## 三、变更明细

### 3.1 菜单重命名（2 条 UPDATE）

| menu_id | 原名 | 新名 |
|---------|------|------|
| 9800 | 竞赛通管理 | **平台数据管理** |
| 9700 | 赛事方工作台 | **赛事管理中心** |

### 3.2 隐藏重复菜单（5 条 UPDATE, visible='1'）

| menu_id | 菜单名 | 重叠对象 | 原因 |
|---------|--------|----------|------|
| 9900 | 渠道管理（根） | 9845-9847 | 9800 下已有完整渠道组 |
| 9901 | 渠道认证管理 | 9846 渠道认证审核 | 同 component: `jst/channel-auth/index` |
| 9902 | 渠道列表 | 9845 渠道管理 | 同 component: `jst/channel/index` |
| 9910 | 数据看板（根） | 9801 | 仅含 9911 一个子菜单 |
| 9911 | 运营数据 | 9801 运营数据看板 | 同 component: `jst/dashboard/index` |

### 3.3 渠道菜单合并（2 条 UPDATE 移动 parent_id）

| menu_id | 菜单名 | 原 parent | 新 parent | order_num | 说明 |
|---------|--------|-----------|-----------|-----------|------|
| 9903 | 绑定管理 | 9900 | **9800** | 63 | 渠道组尾部，`jst/binding/index` 无重叠 |
| 9904 | 预约管理 | 9900 | **9800** | 47 | 订单组尾部，`jst/appointment/index` 不同于 9834 code-gen 页 |

附带 `INSERT IGNORE INTO sys_role_menu` 确保 admin 角色绑定 9903/9904 及其按钮权限（9940-9941, 9950-9953）。

### 3.4 404 排查结果

逐一核对 9700-9897 所有 `menu_type='C'` 菜单的 component 路径与 `src/views/` 下 Vue 文件：

- **9700 系列**（partner）：8 页面全部存在 ✅
- **9800 系列**（jst）：55+ 页面全部存在 ✅
- **9900 系列**（已隐藏）：4 页面全部存在 ✅

**结论：无 404 菜单。**

### 3.5 角色分配验证

| 角色 | 可见范围 | 验证方式 |
|------|----------|----------|
| admin | 9700-9899 全部 | SQL 脚本第 4.5 节 SELECT 验证 |
| jst_partner | 仅 9700-9799 | 由 `99-migration-partner-menus.sql` 绑定 |
| jst_channel | 渠道相关 | 保留原有绑定不变 |

### 3.6 数据看板去重

- **保留**：9801 "运营数据看板"（parent=9800，`jst/dashboard/index`）
- **隐藏**：9910 根 + 9911 子菜单（visible='1'）

## 四、清理前后对比

### 清理前（根级别可见菜单 ≥9700）

```
├── 9910 数据看板          ← 仅 1 子菜单，与 9801 重叠
├── 9800 竞赛通管理        ← 名称不明确
├── 9900 渠道管理          ← 与 9800 下渠道组重叠
└── 9700 赛事方工作台      ← 名称不明确
```

### 清理后

```
├── 9800 平台数据管理      ← 重命名，含渠道/绑定/预约完整树
└── 9700 赛事管理中心      ← 重命名，赛事方专属
```

根级别可见菜单从 **4 个** 减少到 **2 个**，层级清晰。

## 五、执行说明

```bash
# 在测试库执行
mysql -h 59.110.53.165 -P 3306 -u jst_new -pJ8zZpAa4zG8y6a7e jst_new < 架构设计/ddl/99-migration-refactor-menus.sql
```

脚本末尾含 6 条验证 SELECT，执行后检查：
1. 9700/9800 名称是否正确
2. 9900/9901/9902/9910/9911 是否 visible='1'
3. 9903/9904 parent_id 是否为 9800
4. 9800 下可见菜单是否完整
5. admin/jst_partner 角色菜单数量是否正确
6. 根级别仅 9800 + 9700 可见

## 六、风险与注意

| 项 | 说明 |
|----|------|
| 回滚 | 所有变更均为 UPDATE visible/parent_id/menu_name，可逆。回滚只需将 visible 改回 '0'、parent_id 改回 9900、menu_name 改回原值 |
| 权限 | 隐藏菜单的按钮权限（F 类型）不受影响，admin 通过直接 URL 仍可访问 |
| 前端缓存 | 执行后需清除浏览器缓存或重新登录，使 `getRouters()` 拉取最新菜单树 |
| 不涉及 | 不修改 Java 后端代码、不修改 Vue 前端代码、不删除任何数据 |

## 七、DoD 检查

- [x] 菜单重命名 SQL
- [x] 重复菜单隐藏 SQL（5 条）
- [x] 404 修复 SQL — 排查结果：无 404，无需修复
- [x] 渠道菜单合并（9903/9904 移入 9800）
- [x] 角色分配检查（验证 SELECT 已内置）
- [x] 数据看板去重（9910/9911 隐藏）
- [x] 报告交付
