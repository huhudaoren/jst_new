# 任务报告 - F4 学生渠道绑定

## A. 任务前自检（Step 2 答题）
1. 涉及表：`jst_student_channel_binding`、`jst_channel`、`jst_user`
2. 涉及状态机：`SM-15`
3. 涉及权限标识：`jst:user:binding:list`、`jst:user:binding:forceUnbind`；小程序端角色校验：`hasRole('jst_student')`
4. 涉及锁名：`lock:bind:{userId}`
5. 事务边界：`BindingService.switchBinding`、`BindingService.unbindCurrent`、`BindingService.forceUnbind`（均为 `@Transactional(rollbackFor = Exception.class)`）
6. ResVO 字段：`bindingId` / `channelId` / `channelName` / `channelType` / `bindTime` / `unbindTime` / `status`（本接口无敏感字段，无额外脱敏输出）
7. 复用样板：`RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/service/impl/ParticipantClaimServiceImpl.java`

## B. 交付物清单
新增文件:
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/enums/BindingStatus.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/dto/SwitchBindingReqDTO.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/dto/UnbindReqDTO.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/dto/BindingQueryReqDTO.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/vo/BindingVO.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/mapper/BindingMapperExt.java`
- `RuoYi-Vue/jst-user/src/main/resources/mapper/user/BindingMapperExt.xml`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/service/BindingService.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/service/impl/BindingServiceImpl.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/controller/wx/WxBindingController.java`
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/controller/BindingAdminController.java`

修改文件:
- `RuoYi-Vue/jst-common/src/main/java/com/ruoyi/jst/common/exception/BizErrorCode.java`（调整 `JST_USER_BIND_CONFLICT` 为 `10011`，追加 `JST_USER_BIND_NOT_FOUND(10012)`）
- `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/auth/impl/WxAuthServiceImpl.java`（补充小程序登录角色注入，保证 `hasRole('jst_student')` 生效）
- `架构设计/ddl/99-test-fixtures.sql`（追加用户 `1004`、渠道 `2002`、初始绑定 `5001`）
- `test/api-tests.http`（追加 `### F4-0 ~ F4-7` 测试块）

## C. mvn compile 结果
```text
[INFO] BUILD SUCCESS
[INFO] Total time:  41.812 s
```

## D. .http 测试结果
- 已追加 F4 测试块：`F4-0 ~ F4-7`
- 本次未实际执行 `.http` 联调
- 原因：当前线程内未成功拉起本地 `ruoyi-admin` 服务；`mvn -f ruoyi-admin/pom.xml spring-boot:run -DskipTests` 在超时前未确认到 `8080` 监听

## E. 遗留 TODO
- 启动本地 `ruoyi-admin`，确认数据库已导入 `99-test-fixtures.sql` 后，执行 `test/api-tests.http` 中 `F4-0 ~ F4-7`

## F. 我做了任务卡之外的什么
- 在 `WxAuthServiceImpl` 补充了小程序登录角色注入（`jst_student` / `jst_channel`），否则 F4 新增接口上的 `@PreAuthorize("@ss.hasRole('jst_student')")` 无法实际通过

## G. 自检清单确认（16-安全文档 §8）
- [x] 所有方法 `@PreAuthorize`
- [x] ReqDTO 有 JSR-303
- [x] 出参用 ResVO
- [x] 敏感字段脱敏
- [x] 列表/当前用户接口已做归属校验（`SecurityCheck.assertSameUser`）
- [x] 写操作有 `@OperateLog`
- [x] 状态机方法 `@Transactional`
- [x] 高并发方法 `jstLockTemplate.execute`
- [x] 没有 `${}` 拼 SQL
- [x] 没有打印明文敏感字段
