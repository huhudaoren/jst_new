# 任务报告 - F5 赛事方入驻审核

## A. 任务前自检（Step 2 答题）
1. 涉及表：`jst_event_partner_apply`、`jst_event_partner`、`sys_user`、`sys_user_role`、`sys_role`
2. 涉及状态机：`SM-4`
3. 涉及权限标识：`@Anonymous`、`jst:organizer:partnerApply:list`、`jst:organizer:partnerApply:detail`、`jst:organizer:partnerApply:approve`、`jst:organizer:partnerApply:reject`、`jst:organizer:partnerApply:supplement`
4. 涉及锁名：`lock:org:apply:{applyId}`
5. 事务边界：`PartnerApplyServiceImpl.submitPublicApply`、`PartnerApplyServiceImpl.approve`、`PartnerApplyServiceImpl.reject`、`PartnerApplyServiceImpl.supplement`，均为 `@Transactional(rollbackFor = Exception.class)`
6. ResVO 字段：
   - 提交：`applyId` / `applyNo`
   - 公共状态：`applyStatus` / `auditRemark` / `submitTime` / `auditTime`
   - 审核通过：`partnerId` / `userId`
   - 列表/详情：包含申请单基础字段，`contactMobile` 已脱敏；详情中的 `settlementInfoJson` 里 `bankAccountNo` 也做了递归脱敏
7. 复用样板：`RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/service/impl/ParticipantClaimServiceImpl.java`

## B. 交付物清单
新增核心文件：
- `RuoYi-Vue/jst-organizer/src/main/java/com/ruoyi/jst/organizer/service/impl/PartnerApplyServiceImpl.java`
- `RuoYi-Vue/jst-organizer/src/main/java/com/ruoyi/jst/organizer/service/PartnerApplyService.java`
- `RuoYi-Vue/jst-organizer/src/main/java/com/ruoyi/jst/organizer/controller/public_/PublicPartnerApplyController.java`
- `RuoYi-Vue/jst-organizer/src/main/java/com/ruoyi/jst/organizer/controller/PartnerApplyAdminController.java`
- `RuoYi-Vue/jst-organizer/src/main/java/com/ruoyi/jst/organizer/mapper/PartnerApplyMapperExt.java`
- `RuoYi-Vue/jst-organizer/src/main/resources/mapper/organizer/PartnerApplyMapperExt.xml`
- `RuoYi-Vue/jst-organizer/src/main/java/com/ruoyi/jst/organizer/enums/PartnerApplyStatus.java`

新增 DTO/VO：
- `PartnerApplyReqDTO`
- `ApproveReqDTO`
- `RejectReqDTO`
- `SupplementReqDTO`
- `PartnerApplyQueryDTO`
- `PartnerApplyListVO`
- `PartnerApplyDetailVO`
- 额外补充：`PartnerApplySubmitResVO`、`PartnerApplyStatusResVO`、`PartnerApplyApproveResVO`

修改文件：
- `RuoYi-Vue/jst-organizer/src/main/java/com/ruoyi/jst/organizer/mapper/SysUserExtMapper.java`
- `RuoYi-Vue/jst-organizer/src/main/resources/mapper/organizer/SysUserExtMapper.xml`
- `RuoYi-Vue/jst-organizer/pom.xml`
- `RuoYi-Vue/jst-common/src/main/java/com/ruoyi/jst/common/exception/BizErrorCode.java`
- `RuoYi-Vue/jst-event/src/main/java/com/ruoyi/jst/event/domain/JstEventPartner.java`
- `RuoYi-Vue/jst-event/src/main/resources/mapper/event/JstEventPartnerMapper.xml`
- `架构设计/ddl/99-test-fixtures.sql`
- `test/api-tests.http`

## C. mvn compile 结果
- `mvn -pl jst-organizer -am compile -DskipTests`：`BUILD SUCCESS`
- `mvn compile -DskipTests`：18 模块 `BUILD SUCCESS`
- 全量编译总耗时：`32.214 s`

## D. .http 测试结果
- F5-1 阻塞：`POST /jst/public/organizer/partner/apply` 实测返回 `401`
- F5-2 阻塞：`GET /jst/public/organizer/partner/apply/{applyNo}/status` 实测返回 `401`
- F5-3 未继续执行：依赖 F5-1 先打通
- F5-4 / F5-5 未完成 HTTP 回归：未在子 Agent 边界内继续绕过项目现有匿名放行故障

补充证据：
- 仓内已有 `RuoYi-Vue/jst-user/src/main/java/com/ruoyi/jst/user/controller/wx/WxAuthController.java` 的 `POST /jst/wx/auth/login` 也实测返回 `401`
- 说明阻塞点是项目现有 `@Anonymous -> PermitAllUrlProperties` 放行链路失效，不是 F5 单独引入的问题

## E. 遗留 TODO
- 需要主 Agent 或框架负责人修复 `RuoYi-Vue/ruoyi-framework/src/main/java/com/ruoyi/framework/config/properties/PermitAllUrlProperties.java` 这条匿名白名单机制
- 修复后应重新执行 `test/api-tests.http` 中 F5-1 ~ F5-5
- `lock:org:apply:{applyId}` 已按任务卡实现，但 `15-Redis-Key与锁规约.md` 当前未见该锁登记，建议由主 Agent 统一补规约

## F. 我做了任务卡之外的什么
- 追加了 3 个必需 ResVO：`PartnerApplySubmitResVO`、`PartnerApplyStatusResVO`、`PartnerApplyApproveResVO`
- 扩展了现有 `SysUserExtMapper`，而不是新建重复 Mapper
- 为对接既有 DDL，在 `jst-event` 的实体/XML 中补齐了 `jst_event_partner.user_id` 映射
- 在 `jst-organizer/pom.xml` 额外加入了 `jst-event` 依赖，否则无法按规约通过 Service 调用赛事方主表创建逻辑

## G. 自检清单确认
- [x] admin 方法已加 `@PreAuthorize`
- [x] public 方法已加 `@Anonymous`
- [x] ReqDTO 有 JSR-303
- [x] 出参使用 ResVO
- [x] 敏感字段已脱敏
- [x] 写操作已加 `@OperateLog`
- [x] 状态机写方法已加 `@Transactional`
- [x] 审核并发写已使用 `jstLockTemplate.execute`
- [x] 状态流转已做 `SM-4` 校验 + 乐观锁更新
- [x] 没有 `${}` 拼 SQL
- [x] 没有打印明文敏感字段

当前结论：F5 代码已实现并通过全量编译，但 `.http` 验收被项目现有匿名放行故障阻塞，建议先由主 Agent 处理框架级 `@Anonymous` 问题后再做最终回归。
