# 任务卡 C8 - 积分商城兑换

## 阶段 / 模块
阶段 C / **jst-points** (商品、兑换订单) + **jst-marketing**（优惠券入库联动）

## 业务背景

PRD 7.5 / 8.7 积分商城：学生用积分（可叠加现金补差）兑换虚拟权益（优惠券/权益卡）或实物商品（周边/礼品）。
- 虚拟商品兑换即时到账（自动发券 / 写 user_rights）
- 实物走发货流程
- 售后退款按"现金→积分"顺序回滚，参考 C4 退款范式

## CCB 决策（我已定）

| # | 决策 | 选择 |
|---|---|---|
| 1 | 支付方式 | 纯积分 / 积分+现金混合（复用 C2 支付通道） |
| 2 | 现金部分 | 走微信支付 Mock，沿用 C2 的 `WxPayService` |
| 3 | 虚拟商品类型 | `coupon` (写 jst_user_coupon)、`rights` (写 jst_user_rights)、`points_card` (暂不做) |
| 4 | 库存 | `jst_mall_goods.stock` 字段，实物必填；虚拟商品 stock=-1 表不限 |
| 5 | 积分冻结 | 下单冻结 (`frozen_points`)，支付成功确认扣减，取消/退款解冻 |
| 6 | 乐观锁 | `jst_points_account.version` |
| 7 | 售后 | 本期支持用户发起"退款退货"，虚拟商品券已 used 不可退 |
| 8 | 发货 | 本期 admin 手动填快递单号，状态 `pending_ship → shipped → completed` |
| 9 | 零积分商品 | price_points=0 且 price_cash>0 是纯现金兑换，允许 |

## 必读

1. PRD § 7.5 优惠券/权益/积分商城 + § 8.7 积分商城
2. `架构设计/ddl/05-jst-points.sql` 表 `jst_mall_goods` / `jst_mall_exchange_order` / `jst_points_account`
3. `架构设计/ddl/07-jst-marketing.sql` 表 `jst_coupon` / `jst_user_coupon` / `jst_user_rights`
4. `架构设计/11-状态机定义.md` § SM-18 (兑换订单)
5. **C2 OrderServiceImpl** 的积分冻结/乐观锁写法（直接复用模式）
6. **C4 RefundServiceImpl** 的回退顺序写法
7. 原型：`小程序原型图/points-mall.*`、`points-goods-detail.html`、`points-order.html`、`coupon-center.html`

## 涉及表

**写**：`jst_mall_goods`（admin CRUD）、`jst_mall_exchange_order`、`jst_points_account`、`jst_points_ledger`、`jst_user_coupon`（虚拟券发放 lookup）、`jst_user_rights`（权益发放 lookup）、`jst_payment_record`（现金部分，跨模块 lookup jst-order）

## 状态机 SM-18

```
pending_pay → paid → pending_ship → shipped → completed
     ↓        ↓
 cancelled  aftersale → refunded / closed
```
虚拟商品省略 `pending_ship/shipped`，`paid → completed` 直接跳。

## 锁
- `lock:mall:goods:stock:{goodsId}` — 库存扣减（等 3s，持 5s）
- `lock:mall:exchange:{orderId}` — 状态推进互斥（等 2s，持 5s）
- `lock:points:freeze:{userId}` — 复用 C2 积分冻结锁

## 权限

**admin（商品管理）**
- `jst:points:mall:goods:list/add/edit/remove/publish`
- `jst:points:mall:exchange:list/ship`（发货）

**wx 学生**
- `jst:points:mall:browse`（匿名可访问部分列表接口用 `@Anonymous`）
- `jst:points:mall:exchange:apply`（下单）
- `jst:points:mall:exchange:my`（我的兑换订单）
- `jst:points:mall:exchange:cancel`

## 接口

### admin 侧
- GET/POST/PUT/DELETE `/jst/points/mall/goods` CRUD
- POST `/jst/points/mall/goods/{id}/publish` 上架
- POST `/jst/points/mall/goods/{id}/offline` 下架
- GET `/jst/points/mall/exchange/list` 所有兑换订单分页
- POST `/jst/points/mall/exchange/{orderId}/ship` 填快递单号

### wx 侧
- GET `/jst/wx/mall/goods/list?category=&pageNum=` 商品列表
- GET `/jst/wx/mall/goods/{id}` 详情
- POST `/jst/wx/mall/exchange/apply` ⭐ 兑换下单
  - 入参 `{ goodsId, quantity, addressId?, payMethod }`
  - 逻辑（@Transactional）：
    1. 锁 `lock:mall:goods:stock:{goodsId}`
    2. 查商品 FOR UPDATE，校验 status=on、stock 足够、用户角色符合 role_limit
    3. 计算 `totalPoints = price_points × qty`、`totalCash = price_cash × qty`
    4. 锁 `lock:points:freeze:{userId}`
    5. 乐观锁冻结积分：`UPDATE jst_points_account SET frozen_points = frozen_points + totalPoints, available_points = available_points - totalPoints, version=version+1 WHERE user_id=? AND version=? AND available_points >= totalPoints`
    6. 写 points_ledger 一行 `type='frozen'`
    7. INSERT `jst_mall_exchange_order` status=`pending_pay`（纯积分则直接 `paid` 触发发放）
    8. 若 totalCash > 0：调 `WxPayService` mock 路径；本期 wx 前端后续调 mock-success 完成
    9. 若 totalCash == 0：直接内部推进 `paid` → 若虚拟商品直接 `completed`（发券/权益）；若实物则 `pending_ship`
    10. 扣减 `jst_mall_goods.stock`
    11. 返回 ResVO
- POST `/jst/wx/mall/exchange/{orderId}/pay/mock-success` 积分+现金的现金部分支付成功回调（复用 C2 通道）
  - 推进状态 `pending_pay → paid`，并根据商品类型决定 `paid → completed`（虚拟即发）或 `paid → pending_ship`（实物）
  - 虚拟商品发放：INSERT user_coupon / user_rights（跨模块 lookup）
  - 积分从 frozen → deducted（points_ledger 写 `consumed`）
- POST `/jst/wx/mall/exchange/{orderId}/cancel`
  - pending_pay 可取消，积分从 frozen 解冻回 available，恢复库存
- GET `/jst/wx/mall/exchange/my` 我的兑换订单列表
- GET `/jst/wx/mall/exchange/{id}` 详情

## 交付物

新增：
- `jst-points/.../enums/MallExchangeStatus.java`（SM-18 + assertCanTransitTo）
- `jst-points/.../enums/MallGoodsType.java`（virtual/physical）
- `jst-points/.../enums/MallGoodsStatus.java`（draft/on/off）
- dto/vo：`GoodsQueryReqDTO`、`ExchangeApplyDTO`、`ExchangeShipDTO`、`GoodsListVO`、`GoodsDetailVO`、`ExchangeListVO`、`ExchangeDetailVO`、`ExchangeApplyResVO`
- mapper ext：`MallGoodsMapperExt`、`MallExchangeOrderMapperExt` + xml
- mapper lookup：
  - `UserCouponLookupMapper`（写 jst-marketing.jst_user_coupon）
  - `UserRightsLookupMapper`（写 jst-marketing.jst_user_rights）
- service：`MallGoodsService`、`MallExchangeService`（含 admin + wx 两套门面）
- controller：`MallGoodsAdminController`、`MallExchangeAdminController`、`WxMallGoodsController`、`WxMallExchangeController`

修改：
- `jst-common/.../BizErrorCode.java` 追加（60001~60020 段）：
  - `MALL_GOODS_NOT_FOUND`
  - `MALL_GOODS_OFFLINE`
  - `MALL_STOCK_INSUFFICIENT`
  - `MALL_POINTS_INSUFFICIENT`
  - `MALL_ROLE_NOT_ALLOWED`
  - `MALL_EXCHANGE_STATUS_INVALID`
  - `MALL_CANCEL_NOT_ALLOWED`
- `架构设计/15-Redis-Key与锁规约.md` 登记 2 把新锁
- `架构设计/ddl/99-test-fixtures.sql` 追加：
  - 3 个商品（虚拟-优惠券 / 虚拟-权益 / 实物-礼品）
  - 1 个积分充足用户（复用 1001 加积分到 10000）
  - 1 个积分不足用户
- `test/wx-tests.http` + `test/admin-tests.http` 追加 C8 段

## 测试

**wx**
- C8-W1 浏览商品列表
- C8-W2 商品详情
- C8-W3 兑换虚拟-优惠券 happy path（积分冻结 → 即发）
- C8-W4 兑换虚拟-权益 happy path
- C8-W5 兑换实物商品 happy path（状态停在 pending_ship）
- C8-W6 积分不足应失败
- C8-W7 库存不足应失败
- C8-W8 角色不匹配应失败（channel 试兑 student 专属商品）
- C8-W9 pending_pay 取消（解冻积分、恢复库存）
- C8-W10 paid 取消应失败
- C8-W11 并发兑最后 1 库存（一成一败）

**admin**
- C8-A1 商品 CRUD + 上架下架
- C8-A2 兑换订单列表
- C8-A3 发货（pending_ship → shipped）
- C8-A4 完成（shipped → completed，学生侧"确认收货"也可，本期 admin 代做）

## DoD
- [ ] mvn compile SUCCESS
- [ ] .http 全绿
- [ ] 虚拟商品发放能在 jst_user_coupon / jst_user_rights 查到记录
- [ ] 积分账户 available/frozen 变化正确
- [ ] 任务报告 `C8-积分商城兑换-回答.md`
- [ ] commit: `feat(jst-points): C8 积分商城兑换 (SM-18)`

## 不许做
- ❌ 不许改 jst_points_account 的 version 乐观锁协议
- ❌ 不许新建 WxPayService 的第三个实现，复用现有 Mock/Real
- ❌ 不许跨 import jst-marketing entity，走 lookup mapper
- ❌ 不许本期做"物流查询"接入真实物流 API
- ❌ 不许本期做"售后退款"完整流程（留 C9 或下一期）

## 依赖：C2（支付通道）✅
## 优先级：中

---
派发时间：2026-04-09
