<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.jst.marketing.mapper.lookup.MarketingUserLookupMapper">

    <select id="selectByUserId" resultType="java.util.LinkedHashMap">
        select user_id as userId,
               user_type as userType,
               status,
               mobile,
               nickname
        from jst_user
        where user_id = #{userId}
        limit 1
    </select>

    <select id="selectByUserIds" resultType="java.util.LinkedHashMap">
        select user_id as userId,
               user_type as userType,
               status,
               mobile,
               nickname
        from jst_user
        where user_id in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        order by user_id asc
    </select>
</mapper>
