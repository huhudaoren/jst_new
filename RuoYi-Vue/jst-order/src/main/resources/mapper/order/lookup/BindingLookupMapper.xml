<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.jst.order.mapper.lookup.BindingLookupMapper">

    <select id="selectActiveBinding" resultType="map">
        select b.binding_id as bindingId,
               b.channel_id as channelId
        from jst_student_channel_binding b
        where b.user_id = #{userId}
          and b.status = 'active'
          and b.del_flag = '0'
        order by b.binding_id desc
        limit 1
    </select>
</mapper>
